# 패치 기록

지금까지는 버전을 따로 표시하지 않고 진행해왔기 때문에, 현재 상태를 **v0.1**로 잡고 여기서부터 기록을 시작한다. 그래서 v0.1 항목은 이전 버전과의 "변경점" 목록이 아니라, 이 시점까지 만들어진 게임 시스템과 구성 전체를 정리한 스냅샷이다. **v0.2부터는 실제 패치 노트(추가/변경/수정) 형식으로 기록한다.**

세부 수치(확률, 가격, 릴링 난이도 등)는 여기서 반복하지 않는다 — [README.md](README.md)를 참고.

---

## v0.2

### 추가

- 낚싯대 등급업 시스템: 등급별 강화재료(희귀 낚싯대 강화재료 3개, 특급 낚싯대 강화재료 5개)를 모아 다음 등급으로 등급업. 재료는 현재 등급으로 꽝이 아닌 물고기를 낚을 때마다 확률적으로 드랍되며, 드랍률은 낚싯대 레벨에 비례해 5%(레벨 1) ~ 23%(레벨 10)로 오른다. 재료를 얻으면 낚시 결과창 다음에 별도 팝업으로 안내된다. 등급업하면 재료는 소모되고 레벨은 새 등급의 1레벨로 초기화된다.
- 낚싯대 등급에 따른 저등급 캐스팅 스킵: 희귀 낚싯대는 꽝을, 특급 낚싯대는 꽝+일반을 더 이상 낚지 않음(나머지 등급 확률로 재분배). 설정에 "저등급 캐스팅 스킵" on/off 토글 추가(기본 켜짐).
- 낚시꾼 능력치 3종(근력/행운/정밀함), 각 0~5단계, 낚싯대와는 별개로 상점 업그레이드 탭 하단에서 강화. 근력은 히트당 제한시간을, 정밀함은 캐스팅 바 속도를(느리게), 행운은 희귀·특급·전설 출현 확률을 각각 단계당 늘려준다. 강화 비용은 300 → 500 → 900 → 1,500 → 2,500(합 5,700개).
- 업그레이드 탭에 레벨/단계를 채워지는 칸(pip) 형태로 표시(낚싯대 10칸, 능력치 각 5칸) — 낚싯대 옆에는 강화재료 보유/필요 개수도 항상 같이 표시.
- 로컬 전용 개발자 패널(`dev-mode.js`, gitignored, PC마다 새로 생성): 등급별 강제 캐스팅 버튼, 저장 데이터 초기화 버튼.
- `AGENT.md` 신설(게임 내용이 아니라 작업 방식 규칙 정리용, gitignored라 저장소에는 안 올라감).

### 변경

- 낚싯대 레벨의 효과를 릴링 존 높이로만 축소(이전엔 제한시간·판정 오차에도 영향) — 제한시간과 판정 오차(양쪽 합 15%)는 이제 등급과 무관한 고정값.
- 낚싯대 레벨업 가격을 100 단위로 반올림하고, 등급별 기준가를 달리해서 상위 등급으로 갈수록 레벨 1~10 총비용이 더 커지도록 조정(일반 16,900 / 희귀 33,900 / 특급 67,800).
- 업그레이드 탭에서 "최고 레벨이에요" 등 설명 문구를 제거 — 재료 카운터로 정보 대체.

### 문서

- README.md에 낚싯대 등급업/저등급 스킵/낚시꾼 능력치 섹션 추가.

---

## v0.1 — 시스템 개요

### 파일 구성

| 파일 | 역할 |
|---|---|
| `index.html` | 화면 구조. 게임 캔버스, 상단바/하단바, 릴링 게이지, 상점/보관함/설정/결과 팝업 마크업 |
| `fish-data.js` | 물고기·꽝 아이템·등급·낚싯대 데이터와 순수 계산 함수(`pickCatch`, `priceForCatch`, `rodLevelCost` 등). DOM을 건드리지 않는 데이터 전용 모듈 |
| `game.js` | 배경 렌더링(캔버스), 캐스팅~릴링~결과 루프, 상점/보관함/설정 UI 로직, 저장, 사운드를 모두 담은 메인 스크립트 |
| `style.css` | 전체 스타일. `--vw-px`/`--vh-px` 커스텀 프로퍼티로 실제 뷰포트 크기를 반영해 모바일/데스크톱 모두 대응 |
| `icons/*.svg` | 이모지 대신 사용하는 직접 제작한 아이콘들(플랫폼별 이모지 렌더링 차이를 피하기 위함) |
| `manifest.json`, `sw.js`, `icons/icon-*.png` | PWA 설정 — 홈 화면 추가 시 앱처럼 실행 가능 |
| `dev-mode.js` | 로컬 전용 개발자 패널. `.gitignore`에 등록되어 저장소에는 올라가지 않으며, 특정 등급을 강제로 낚는 등 테스트용 기능을 제공 |

### 배경/연출

돌다리 밑 여울을 캔버스로 직접 그린다(이미지 에셋 없음). 물결, 반짝임, 배경을 지나다니는 물고기 실루엣, 빛줄기 등이 매 프레임 애니메이션되고, 화면 전체에 약간의 확대(zoom) 변환이 걸려 있어 탭 좌표를 역변환해서 사용한다.

### 캐스팅 ~ 릴링 ~ 결과 루프

탭 → 대기 → 입질 → 릴링 → 결과의 5단계 상태 기계로 동작한다. 릴링은 세로로 왕복하는 인디케이터를 정해진 구간(존) 안에서 탭해 히트를 쌓는 방식이며, 히트당 제한시간과 허용 미스 횟수가 있다.

캐스팅 바의 물고기 아이콘 색이 등급을 서서히 드러내는 "등급업 연출"이 핵심 연출 요소로, 히트할 때마다 다음 창(window)의 등급이 미리 공개되고 캐스팅 바의 속도도 그 등급을 따라간다. 아직 한 번도 캐스팅해본 적 없는 플레이어에게는 화면 중앙에 캐스팅 위치를 알려주는 튜토리얼 힌트가 뜬다.

### 등급 / 보상 시스템

물고기는 꽝을 포함한 5개 풀(꽝/일반/희귀/특급/전설)로 나뉘고, 각 풀은 자체 출현 확률·가격 구간·릴링 난이도를 가진다. 가격은 등급 구간 내에서 크기에 비례하며, 등급 구간끼리는 겹치지 않도록 설계되어 있다. 잡은 물고기는 즉시 조개로 바뀌지 않고 보관함에 쌓이며, 상점에서 직접 팔아야 한다.

### 상점

구매/판매/업그레이드 3개 탭으로 구성된 팝업(화면 비율에 비례한 고정 크기, 탭을 바꿔도 크기가 바뀌지 않음).

- 구매: 미구현(준비 중 안내만 표시)
- 판매: 보관함에 있는 미판매 물고기를 하나씩 조개로 교환
- 업그레이드: 낚싯대 강화(레벨 1~10) 및 등급업(현재는 상위 등급 두 가지까지 존재하며, 등급업에 필요한 강화재료 시스템은 아직 없어 레벨 10에서 잠겨 있음)

### 보관함

보관함(미판매 물고기 목록)과 도감(종별 등급·잡은 횟수·최고 크기 기록) 2개 탭. 도감은 판매 여부와 무관하게 영구적으로 기록되며, 아직 잡아보지 못한 종은 이름이 "???"로 가려진다.

### 설정

왼손 모드(플레이 HUD 좌우 반전), 효과음 볼륨 조절, 진행 데이터 전체 삭제(3초 대기 후에만 확정 가능한 이중 확인)를 제공한다.

### 저장 / 사운드

- 로그인 없이 브라우저 `localStorage`에 저장(조개, 낚싯대, 보관함, 도감, 첫 캐스팅 여부, 왼손 모드, 효과음 볼륨을 각각 별도 키로 관리).
- 효과음은 이미지/오디오 파일 없이 WebAudio로 그때그때 생성(캐스팅, 입질, 히트, 미스, 성공, 실패, 첨벙, 동전 소리).

### 안정성

초기화 스크립트 전체가 `try/catch`로 감싸여 있어, 초기화 중 어떤 오류가 나도 검은 화면으로 멈추는 대신 화면에 에러 메시지를 직접 표시한다(원인 파악용 스크린샷을 남기기 위함).
